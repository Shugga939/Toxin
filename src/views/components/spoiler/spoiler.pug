include Views/components/checkbox/checkbox

mixin spoiler({type, typeContent, content})
  .spoiler
    .spoiler__head
      h3.spoiler__title 
        | Дополнительные удобства
      span.spoiler__status
        include Images/icons/expand-more.svg
    .spoiler__content
      if type == 'checkboxes'
        +checkbox({
          type: typeContent,
          items: content
        })

    